# Auction System 拍卖行系统

本项目使用 Python + Flask + SQLAlchemy 构建，用于拍卖物品的数据管理、图片处理与上传等功能。

## 📦 环境需求

- Python 3.10+
- Windows（需要 pywin32）
- SQLite（已随 Python 内置）

## 📚 依赖安装

项目使用 `requirements.txt` 统一管理依赖库。

一次性安装所有库：

```bash
pip install -r requirements.txt
